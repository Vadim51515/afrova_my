import { Link } from '../../../common/ui-components/Link';

export const ProfileBtn = () => {
    // const aaaa = useSelector();
    const a = 1;

    return (
        <Link
            href='/profile'
            pattern='primary'
        >
            profile
        </Link>
    );
};
