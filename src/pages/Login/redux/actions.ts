import { loginSliceActions } from './slice';

const {
    setLogin,
    setPassword,
    setStatus,
} = loginSliceActions;

export const loginActions = {
    setLogin,
    setPassword,
    setStatus,
};
